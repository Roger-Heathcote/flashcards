console.log("Some day this script will build the .md files in ./cards into files that can be imported into Anki, and/or other flashcard formats. Til then it assumes you are on some form of unix and have mdankideck installed. Run `npm run build` to build the Anki decks.")